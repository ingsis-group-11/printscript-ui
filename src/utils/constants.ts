export const FRONTEND_URL = process?.env?.FRONTEND_URL ?? "https://printscript-dev.brazilsouth.cloudapp.azure.com"
export const BACKEND_URL = process?.env?.BACKEND_URL ?? "http://localhost:8080/api"
export const AUTH0_USERNAME = process?.env?.AUTH0_USERNAME ?? "mbarreiro@mail.austral.edu.ar"
export const AUTH0_PASSWORD = process?.env?.AUTH0_PASSWORD ?? "Astor2024$"
